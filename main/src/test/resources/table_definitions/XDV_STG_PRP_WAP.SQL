CREATE GLOBAL TEMPORARY TABLE XDV_STG_PRP_WAP (
    USER_AGENT_HEADER    VARCHAR2(256 BYTE),
    EVENT_LINKAGE_ID     INTEGER
) ON COMMIT DELETE ROWS;

CREATE INDEX XDV_STG_PRP_WAP_IDX_LNKG ON XDV_STG_PRP_WAP (EVENT_LINKAGE_ID);

ALTER TABLE XDV_STG_PRP_WAP ADD (
  PRIMARY KEY (EVENT_LINKAGE_ID));
