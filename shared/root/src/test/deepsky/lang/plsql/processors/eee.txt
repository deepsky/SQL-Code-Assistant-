SELECT
  DURATION_,
  IMEI,
  GSM_SUBSCIBER_NUMBER,
  GSM_SUBSCRIBER_IMSI,
  PSTN_ROUTE,
  START_CELL_ID,
  END_CELL_ID,
  l_msc_id,
  DISCONNECT_TIME,
  LAC,
  CALL_DATE,
  ANSWER_TIME,
  CALL_SEQUENCE_NUMBER,
  SERVICE_TYPE,
  TRAFFIC_CLASS,
  CALL_SETUP_RESULT_CODE,
  TERMINATING_REASON,
  RECORD_TYPE,
  FLOOR(xdv_date_pkg.date_to_sec(FROM_TZ(CALL_DATE + NUMTODSINTERVAL(EXTRACT, 'HOUR') + NUMTODSINTERVAL(EXTRACT, 'MINUTE') + NUMTODSINTERVAL(EXTRACT, 'SECOND'), TZ) AT TIME ZONE 'GMT') / 3600) * 3600,
  MOD(xdv_date_pkg.date_to_sec(FROM_TZ(CALL_DATE + NUMTODSINTERVAL(EXTRACT, 'HOUR') + NUMTODSINTERVAL(EXTRACT, 'MINUTE') + NUMTODSINTERVAL(EXTRACT, 'SECOND'), TZ) AT TIME ZONE 'GMT'), 3600),
  XDV_AMACS_TRN_SEQ.NEXTVAL
FROM
  XDV_SINGTEL_AMA_CS_ET
